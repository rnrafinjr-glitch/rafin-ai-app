import streamlit as st

st.set_page_config(page_title="রাফিনের এআই", page_icon="🤖")
st.title("🤖 এম ডি রাফিনের এআই বন্ধু")

user_input = st.text_input("এখানে আপনার প্রশ্ন লিখুন:", placeholder="যেমন: তোমার নাম কি?")

if st.button("উত্তর দিন"):
    if user_input:
        msg = user_input.lower()
        if "নাম" in msg:
            st.info("আমার নাম রাফিন-এআই।")
        elif "তৈরি" in msg or "মালিক" in msg:
            st.success("আমাকে তৈরি করেছেন এম ডি রাফিন।")
        elif "কেমন আছো" in msg or "কেমন আছ" in msg:
            st.write("আমি খুব ভালো আছি! আপনি কেমন আছেন?")
        else:
            st.warning("আমি এটি এখনো শিখিনি। রাফিনকে বলুন আমাকে শিখিয়ে দিতে!")
    else:
        st.error("দয়া করে কিছু লিখুন!")
import streamlit as st

st.set_page_config(page_title="রাফিনের বিশ্বকোষ", page_icon="🇧🇩")
st.title("🇧🇩 এম ডি রাফিনের তথ্য ভাণ্ডার")

# বাংলাদেশ বিভাগ
st.header("📌 একনজরে বাংলাদেশ")
st.info("**আয়তন:** ১,৪৭,৫৭০ বর্গ কি.মি.")
st.success("**রাজধানী:** ঢাকা")
st.warning("**ভাষা:** বাংলা")

st.divider()

# চ্যাট বক্স
st.header("🤖 আমাকে প্রশ্ন করুন")
user_input = st.text_input("এখানে লিখুন (যেমন: বাংলাদেশের জাতীয় মাছ কি?)")

if st.button("উত্তর দিন"):
    if "মাছ" in user_input:
        st.write("✅ বাংলাদেশের জাতীয় মাছ ইলিশ।")
    elif "তৈরি" in user_input:
        st.write("✅ আমাকে তৈরি করেছেন এম ডি রাফিন।")
    else:
        st.write("আমি এটি শিখছি, রাফিনকে বলুন আমাকে শিখিয়ে দিতে!")
