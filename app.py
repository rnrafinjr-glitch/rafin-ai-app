import streamlit as st

st.set_page_config(page_title="রাফিনের এআই", page_icon="🤖")
st.title("🤖 এম ডি রাফিনের এআই বন্ধু")

user_input = st.text_input("এখানে লিখুন:")

if st.button("উত্তর দিন"):
    if user_input:
        msg = user_input.lower()
        if "তৈরি" in msg or "মালিক" in msg:
            st.success("আমাকে তৈরি করেছেন এম ডি রাফিন।")
        elif "কেমন আছো" in msg:
            st.info("আমি খুব ভালো আছি! আপনি কেমন আছেন?")
        else:
            st.warning("আমি এটি এখনো শিখিনি।")
